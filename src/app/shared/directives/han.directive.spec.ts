import { HanDirective } from './han.directive';

describe('HanDirective', () => {
  it('should create an instance', () => {
    const directive = new HanDirective();
    expect(directive).toBeTruthy();
  });
});
