import { WookieeDirective } from './wookiee.directive';

describe('WookieeDirective', () => {
  it('should create an instance', () => {
    const directive = new WookieeDirective();
    expect(directive).toBeTruthy();
  });
});
